void Split(){
    // 1. Invoke ChooseSplitAxis to determine the axis,
    // perpendicular to which the split is performed

    // 2. Invoke ChooseSplitIndex to determine the best
    // distribution into two groups along that axis

    // 3. Distribute the entries into two groups
}