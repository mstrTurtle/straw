void OverflowTreatment(){
    // If the level is not the root level and this is the first
    // call of OverflowTreatment in the given level
    // during the insertion of one data rectangle, then
    //    invoke ReInsert
    // else
    //     invoke Split
    // end
}